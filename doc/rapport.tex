\documentclass[12pt,a4paper]{article}

% --- Packages de base ---
\usepackage[utf8]{inputenc}
\usepackage[french]{babel}
\usepackage[T1]{fontenc}
\usepackage{geometry}
\geometry{top=2.5cm, bottom=2.5cm, left=2.5cm, right=2.5cm}

% --- Esthétique & Polices ---
\usepackage{lmodern}
\usepackage{sectsty}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{xcolor}
\usepackage{enumitem}

% Couleurs modernes
\definecolor{mainblue}{RGB}{0, 75, 135}
\definecolor{lightgray}{gray}{0.95}

% Styles de sections
\sectionfont{\color{mainblue}\selectfont}
\subsectionfont{\color{mainblue}\selectfont}

% --- Code (Listings) ---
\usepackage{listings}

\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.98,0.98,0.98}

% Definition of YAML language
\newcommand\YAMLcolonstyle{\color{red}\mdseries}
\newcommand\YAMLkeystyle{\color{black}\bfseries}
\newcommand\YAMLvaluestyle{\color{blue}\mdseries}

\makeatletter
\newcommand\language@yaml{yaml}
\expandafter\expandafter\expandafter\lstdefinelanguage
\expandafter{\language@yaml}
{
  keywords={true,false,null,y,n},
  keywordstyle=\color{darkgray}\bfseries,
  basicstyle=\YAMLkeystyle,
  sensitive=false,
  comment=[l]{\#},
  morecomment=[s]{/*}{*/},
  commentstyle=\color{purple}\ttfamily,
  stringstyle=\YAMLvaluestyle\ttfamily,
  moredelim=[l][\color{orange}]{\&},
  moredelim=[l][\color{magenta}]{\*},
  moredelim=**[il][\YAMLcolonstyle{:}\YAMLvaluestyle]{:},
  morestring=[b]',
  morestring=[b]",
  literate =    {---}{{\YAMLkeystyle-{-}-}}3
                {>}{{\YAMLkeystyle>}}1
                {|}{{\YAMLkeystyle|}}1
                {\ -\ }{{\YAMLkeystyle\ -\ }}3,
}
\lst@AddToHook{EveryJob}{\lstset{language=}}
\makeatother

\lstset{
    backgroundcolor=\color{backcolour},   
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\small,
    breakatwhitespace=false,         
    breaklines=true,                 
    captionpos=b,                    
    keepspaces=true,                 
    numbers=left,                    
    numbersep=5pt,                  
    showspaces=false,                
    showstringspaces=false,
    showtabs=false,                  
    tabsize=2,
    frame=lines,
    rulecolor=\color{mainblue}
}

% --- En-tête et Pied de page ---
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\color{gray}Projet SEPA26}
\fancyhead[R]{\color{gray}Rapport Technique}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}

% --- Liens Hypertexte ---
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=mainblue,
    filecolor=magenta,      
    urlcolor=mainblue,
    pdfpagemode=FullScreen,
}

% --- Page de Garde ---
\begin{document}

\begin{titlepage}
    \centering
    \vspace*{5cm}
    {\Huge \bfseries \color{mainblue} Infrastructure de Données et Service REST}\\[0.5cm]
    {\Large Projet SEPA26}\\[2cm]
    
    \vfill
    {\large \today}
\end{titlepage}

\newpage
\tableofcontents
\newpage

\section{Introduction}
Ce document présente la mise en œuvre du service REST SEPA26, incluant la persistance des données et les fonctionnalités de consultation et de gestion des flux bancaires. L'architecture repose sur Spring Boot, MariaDB et les standards XML/XSD.

\section{Gestion de la Base de Données}
Le choix s'est porté sur \textbf{MariaDB}, une base de données relationnelle performante et open-source.

\subsection{Persistance des Données (JPA/Hibernate)}
La persistance est assurée par \textbf{Spring Data JPA}. Le modèle de données \texttt{Document} a été annoté pour correspondre à la structure de la base de données, permettant un mapping automatique entre les objets Java et les tables SQL.

\subsection{Stratégie de Test}
Pour garantir la stabilité du code sans polluer la base de données de production, une base de données \textbf{H2 en mémoire} est utilisée lors de l'exécution des tests. Cette configuration est définie dans le profil \texttt{test} (\texttt{application-test.properties}), permettant des tests rapides, isolés et reproductibles à chaque construction du projet.

\section{Service REST}
Le service expose plusieurs points d'entrée (endpoints) pour la gestion des flux SEPA.

\subsection{Consultation}
\begin{itemize}
    \item \textbf{Accueil} (\texttt{/}) : Présentation du projet et des auteurs.
    \item \textbf{Aide} (\texttt{/help}) : Documentation interactive des API.
    \item \textbf{Résumé} (\texttt{/sepa26/resume/xml} \& \texttt{/html}) : Listes simplifiées des transactions.
    \item \textbf{Détail} (\texttt{/sepa26/xml/\{id\}} \& \texttt{/html/\{id\}}) : Consultation complète d'une transaction, avec transformation XSLT pour le rendu HTML.
\end{itemize}

\subsection{Gestion des flux}
\begin{itemize}
    \item \textbf{Insertion} (\texttt{POST /sepa26/insert}) : Ajout d'un flux après validation par schéma XSD et vérification d'unicité.
    \item \textbf{Suppression} (\texttt{DELETE /sepa26/delete/\{id\}}) : Retrait d'une transaction de la base.
\end{itemize}

\subsubsection*{Exemple de test d'insertion (cURL)}
Pour tester l'insertion, la commande suivante peut être utilisée :
\begin{lstlisting}[language=bash, caption=Commande cURL pour l'insertion]
curl -X POST http://10.130.162.188:8100/sepa26/insert \
-H "Content-Type: application/xml" \
-d '<Document xmlns="http://univ.fr/sepa26">
  <DrctDbtTxInf>
    <PmtId>REF-DOC-123</PmtId>
    <InstdAmt Ccy="EUR">150.00</InstdAmt>
    <DrctDbtTx>
      <MndtRltdInf>
        <MndtId>MANDAT-999</MndtId>
        <DtOfSgntr>2026-03-01</DtOfSgntr>
      </MndtRltdInf>
    </DrctDbtTx>
    <DbtrAgt>
      <FinInstnId>
        <BIC>ROUENSWNXXX</BIC>
      </FinInstnId>
    </DbtrAgt>
    <Dbtr>
      <Nm>Jean Dupont</Nm>
    </Dbtr>
    <DbtrAcct>
      <Id>
        <IBAN>FR7612345678901234567890123</IBAN>
      </Id>
    </DbtrAcct>
  </DrctDbtTxInf>
</Document>'
\end{lstlisting}

\section{Validation et Transformation}
La robustesse du système est garantie par :
\begin{enumerate}
    \item \textbf{Validation XSD} : Chaque flux entrant est validé par rapport au schéma officiel pour garantir l'intégrité des données.
    \item \textbf{Transformation XSLT} : Le rendu HTML des transactions est généré dynamiquement à partir du flux XML via un processeur XSLT.
\end{enumerate}

\section{Conclusion}
L'architecture logicielle choisie offre une séparation claire des responsabilités, une sécurité renforcée des identifiants et une interopérabilité maximale grâce aux standards XML.

\end{document}
